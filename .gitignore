/target

.gitconfig
.gitmodules
.idea
.mcp.json
.profile
.ripgreprc
.zprofile
.zshrc
.vscode
.bashrc
.bash_profile
config
HEAD
hooks
objects
refs
.claude

.ruff_cache
.venv
.worktrees
benchmark/output
benchmark/ispd/raw/
benchmark/ispd/extracted/
benchmark/ispd/manifests/
benchmark/ispd/index.json
build
**/__pycache__/**
# NOTE: this blanket rule also hides real inputs, e.g. demo/blinky.json, which
# the demo flow needs but a fresh clone does not get. Un-ignored explicitly
# below; prefer narrowing this rule to the generated output dirs over time.
*.json
!demo/blinky.json
*.so
perf.data*
/target-diag
